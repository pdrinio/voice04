<Page
    x:Class="Voice04.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Voice04"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="#152951">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

        <MediaElement x:Name="media" AutoPlay="False" />

        <StackPanel Grid.Row="0" Margin="40,0,0,0" VerticalAlignment="Center">
            <TextBlock x:Name="txbEstado" Foreground="White" FontSize="25" Text="Inicializando..."/>
            <TextBlock x:Name="txbSiguientePaso" Foreground="White" FontSize="25" Text="Siguiente paso..."/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="40,0,0,0" VerticalAlignment="Center">
            <TextBlock x:Name="txbConsola" Foreground="White" FontSize="25" Text="Texto escuchado...."/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="40,0,0,0" VerticalAlignment="Center">
            <TextBlock x:Name="txbTextoReconocido" Foreground="White" FontSize="25" Text="Texto escuchado...."/>
        </StackPanel>

    </Grid>
</Page>
